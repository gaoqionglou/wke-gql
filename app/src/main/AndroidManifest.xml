<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wke.gql">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".layoutdemo.activity.CustomTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AActivity" />
        <activity android:name=".BActivity" />
        <activity android:name=".CActivity" />
        <activity android:name=".DemoActivity" />
        <activity android:name=".testCollapsing.activity.CollapsingtoolbarActivity" />
        <activity android:name=".testCollapsing.activity.BootActivity" />
        <activity android:name=".layoutdemo.activity.BottomSheetActivity" />
        <activity android:name=".layoutdemo.activity.MyBehaviorActivity" />
        <activity android:name=".layoutdemo.activity.LayoutSrcollActivity" />
        <activity android:name=".layoutdemo.activity.LayoutActivity" />
        <activity android:name=".MVPdagger2.testWeather.WeatherActivity" />
        <activity android:name=".testCollapsing.activity.BehaviorOneActivity" />
        <activity android:name=".testCollapsing.activity.CustomBehaviorActivity" />
        <activity android:name=".layoutdemo.activity.LayoutBehaviorDemoActivity" />
        <!--<activity android:name=".layoutdemo.activity.CustomTabActivity"></activity>-->
    </application>

</manifest>